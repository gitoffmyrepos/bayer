# Nightwatch — Cloud-Agnostic AI Monitoring Platform
# Core
fastapi>=0.109
uvicorn[standard]>=0.27
pydantic>=2.6
pyyaml>=6.0
structlog>=24.1
httpx>=0.27
aiohttp>=3.9

# LLM Providers (install all — each is optional at runtime)
anthropic>=0.20       # Anthropic Claude
openai>=1.14          # OpenAI + DeepSeek (OpenAI-compatible API)

# AWS Adapter
boto3>=1.34

# ForexTrader Adapter (Kubernetes)
kubernetes>=29.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
